@model IEnumerable<Riggen.Models.ScoreHistoryModel>
@Html.Partial("_AdminSidebar")

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Year)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ScoreOfYear)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Year)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ScoreOfYear)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ScoreHistoryId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ScoreHistoryId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ScoreHistoryId })
        </td>
    </tr>
}

</table>
